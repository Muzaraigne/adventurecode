def containsVoy(chaine):
    voyelles = "aeiou"
    r= 0
    for lettre in chaine:
        if lettre in voyelles:
            r += 1
            if r >= 3:
                return True
    return False

def containsDoubleLetter(chaine):
    for i in range(len(chaine) - 1):
        if chaine[i] == chaine[i + 1]:
            return True
    return False

def containsBadStrings(chaine):
    bad_strings = ["ab", "cd", "pq", "xy"]
    for bad in bad_strings:
        if bad in chaine:
            return True
    return False


def is_nice_string(chaine):
    return (containsVoy(chaine) and containsDoubleLetter(chaine) and not containsBadStrings(chaine))

def is_nice_String_2(chaine):
    rule1 = False
    rule2 = False
    for i in range(len(chaine) - 2):
        if chaine[i] == chaine[i + 2]:
            rule2 = True
        if chaine[i:i + 2] in chaine[i + 2:]:
            rule1 = True
        if rule1 and rule2:
            return True
    return False


def count_nice_strings(strings,methode):
    count = 0
    for string in strings:
        if methode(string):
            count += 1
    return count

if __name__ == "__main__":
    with open("2015/jour5/input.txt", "r") as file:
        strings = file.read().splitlines()

    result = count_nice_strings(strings, is_nice_string)
    result2 = count_nice_strings(strings, is_nice_String_2)
    print(f"Nombre de chaînes de caractères 'nice': {result}")
    print(f"Nombre de chaînes de caractères 'nice' (méthode 2): {result2}")