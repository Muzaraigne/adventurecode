#include <stdio.h>
#include <stdlib.h>
#include "../../utilities.h"


typedef struct List {
    int value;
    struct List *next; // Horaire
    struct List *prev; // Anti-horaire
} List;


int main(void) {
    int player = 466;
    int max_marble = 71436;

	// Ton code ici

	free_lines(input);
	return 0;
}
